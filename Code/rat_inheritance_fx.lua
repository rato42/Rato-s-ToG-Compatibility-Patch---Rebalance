-- function OnMsg.GetCustomFXInheritActorRules(rules)
    -- ApplyEffectToActor_TOG(rules)
-- end

-- function ApplyEffectToActor_TOG(rules)
	-- local suppressor ={
		-- "RAT_TOG_suppressor"
	
	-- }
	-- local compensator = {
		-- "RAT_TOG_suppressor"
	
	-- }
	
	-- for _, tag in ipairs(suppressor) do
        -- rules[#rules + 1] = tag
        -- rules[#rules + 1] = "Suppressor"
    -- end
	
	-- for _, tag in ipairs(compensator) do
        -- rules[#rules + 1] = tag
        -- rules[#rules + 1] = "Compensator"
    -- end
	
-- end